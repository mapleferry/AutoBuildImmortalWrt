name: build-x86-64-immortalwrt-24.10.x

on:
  workflow_dispatch:
    inputs:
      luci_version:
        description: "选择luci版本"
        required: false
        default: "24.10.4"
        type: choice
        options:
          - 24.10.0
          - 24.10.1
          - 24.10.2
          - 24.10.3
          - 24.10.4
      custom_router_ip:
        description: "请设置路由器的管理地址(仅对多网口路由器有效) 格式:192.168.x.1 或 10.x.x.1"
        required: true
        default: "192.168.100.1"
      profile:
        description: '请输入要编译固件大小 单位(MB)'
        required: true
        default: '1024'
      enable_store:
        description: "集成 store 商店"
        required: false
        type: boolean
        default: true
      include_docker:
        description: |
          是否编译 Docker 插件
        required: true
        default: 'yes'
        type: choice
        options:
          - 'yes'
          - 'no'
      enable_pppoe:
        description: "是否配置PPPoE拨号信息?"
        required: true
        default: 'no'
        type: choice
        options:
        - 'yes'
        - 'no'
      pppoe_account:
        description: "宽带账号 (若启用PPPoE)"
        required: false
      pppoe_password:
        description: "宽带密码 (若启用PPPoE)"
        required: false

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Prepare build environment
        run: |
          set -e
          # 检查必要文件
          [ -f "${{ github.workspace }}/x86-64/build24.sh" ] || { echo "❌ build24.sh 不存在"; exit 1; }
          [ -f "${{ github.workspace }}/x86-64/imm.config" ] || { echo "❌ imm.config 不存在"; exit 1; }
          chmod +x ${{ github.workspace }}/x86-64/build24.sh
          
          # 保存路由器IP配置
          mkdir -p "${{ github.workspace }}/custom"
          echo "${{ github.event.inputs.custom_router_ip }}" > "${{ github.workspace }}/custom/custom_router_ip.txt"
          echo "✅ 路由器管理地址: ${{ github.event.inputs.custom_router_ip }}"
          
          # 验证PPPoE输入
          if [[ "${{ github.event.inputs.enable_pppoe }}" == "yes" ]]; then
            [[ -n "${{ github.event.inputs.pppoe_account }}" && -n "${{ github.event.inputs.pppoe_password }}" ]] || {
              echo "❌ 启用PPPoE时必须提供账号和密码"
              exit 1
            }
          fi
      
      - name: Enable Store integration
        run: |
          if [ "${{ github.event.inputs.enable_store }}" == "true" ]; then
            echo 'CUSTOM_PACKAGES="$CUSTOM_PACKAGES luci-app-store"' >> shell/custom-packages.sh
            echo "✅ 已追加 luci-app-store"
          else
            echo "❎ 未启用 luci-app-store"
          fi

      - name: Build ImmortalWrt-x86-64-efi
        run: |
          set -e
          profile="${{ github.event.inputs.profile }}"
          include_docker="${{ github.event.inputs.include_docker }}"
          luci_version="${{ github.event.inputs.luci_version }}"
          
          echo "📦 编译参数: profile=$profile, luci_version=$luci_version, docker=$include_docker"
          
          docker run --rm -i \
            --user root \
            -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
            -v "${{ github.workspace }}/files:/home/build/immortalwrt/files" \
            -v "${{ github.workspace }}/custom:/home/build/immortalwrt/files/etc/config" \
            -v "${{ github.workspace }}/x86-64/imm.config:/home/build/immortalwrt/.config" \
            -v "${{ github.workspace }}/shell:/home/build/immortalwrt/shell" \
            -v "${{ github.workspace }}/x86-64/build24.sh:/home/build/immortalwrt/build.sh" \
            -e PROFILE=$profile \
            -e INCLUDE_DOCKER=$include_docker \
            -e ENABLE_PPPOE=${{ github.event.inputs.enable_pppoe }} \
            -e PPPOE_ACCOUNT=${{ github.event.inputs.pppoe_account }} \
            -e PPPOE_PASSWORD=${{ github.event.inputs.pppoe_password }} \
          immortalwrt/imagebuilder:x86-64-openwrt-${luci_version} /bin/bash /home/build/immortalwrt/build.sh

      - name: Post-build processing
        run: |
          set -e
          luci_version="${{ github.event.inputs.luci_version }}"
          BUILD_DIR="${{ github.workspace }}/bin/targets/x86/64"
          
          # 修复文件权限
          [ -d "${{ github.workspace }}/bin" ] || { echo "❌ bin 目录不存在"; exit 1; }
          docker run --rm -i \
            --user root \
            -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
            immortalwrt/imagebuilder:x86-64-openwrt-${luci_version} \
            chown -R $(id -u):$(id -g) /home/build/immortalwrt/bin
          
          # 验证编译输出
          [ -d "$BUILD_DIR" ] || { echo "❌ 编译输出目录不存在"; exit 1; }
          IMG_COUNT=$(find "$BUILD_DIR" -name "*.img.gz" -type f 2>/dev/null | wc -l)
          QCOW2_COUNT=$(find "$BUILD_DIR" -name "*.qcow2" -type f 2>/dev/null | wc -l)
          [ "$IMG_COUNT" -gt 0 ] || [ "$QCOW2_COUNT" -gt 0 ] || { echo "❌ 未找到编译输出文件"; exit 1; }
          echo "✅ 编译成功: $IMG_COUNT 个 .img.gz, $QCOW2_COUNT 个 .qcow2"

      - name: Create release info
        run: |
          set -e
          RELEASE_TAG=$(date +%Y-%m-%d)
          echo "RELEASE_TAG=$RELEASE_TAG" >> $GITHUB_ENV
          
          cat > ${{ github.workspace }}/router-info.md <<EOF
          ## 📡 路由器后台信息
          <a href="https://wkdaily.cpolar.cn/archives/1">
           <img src="https://img.shields.io/badge/若下载慢可套用国内加速站下载-FC7C0D?logo=github&logoColor=fff&labelColor=000&style=for-the-badge"/>
          </a>
          
          - 🏠 **后台地址**: ${{ github.event.inputs.custom_router_ip }}
          - 👤 **用户名**: root
          - 🔑 **密码**: 无
          EOF
          
          [ "${{ github.event.inputs.include_docker }}" == "yes" ] && \
            echo -e "\n#### 默认带docker" >> ${{ github.workspace }}/router-info.md || \
            echo -e "\n#### 不带docker" >> ${{ github.workspace }}/router-info.md

      - name: Upload ImmortWrt as release assets
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          body_path: ${{ github.workspace }}/router-info.md
          files: |
            ${{ github.workspace }}/bin/targets/x86/64/*.img.gz
            ${{ github.workspace }}/bin/targets/x86/64/*.qcow2
          token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: false